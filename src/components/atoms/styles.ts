import { Paper, styled } from '@mui/material';

export const BootstrapDialog = styled(Paper)(() => ({
	maxHeight: 700,
	width: 741,
	overflow: 'auto',
	padding: '20px',
	border: '1px solid #000000',
}));
